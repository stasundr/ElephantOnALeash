#!/usr/bin/env node

var config = require('./../config');
var app = require('./../app');

app.listen(config.port, function() {
    console.log('Elephant on a leash server is running on port ' + config.port);
});